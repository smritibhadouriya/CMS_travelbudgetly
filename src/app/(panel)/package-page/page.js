'use client';
import PackagePageAdmin from '@/screens/PagesContent/PackagePageAdmin.jsx';
import config from '@/lib/panel-config';

export default function Page() {
  return <PackagePageAdmin config={config} />;
}
